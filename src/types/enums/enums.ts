
enum ArrayFunc {
    Push = "push",
    Unshift = "unshift"
}

enum TextDir {
    LTR = "ltr",
    RTL = "rtl"
}

enum FileFormat {
    ALDT = "aldt",
    PROIEL = "proiel"
}

enum Formalism {
    AGDT = "agdt",
    PROIEL = "proiel",
    UD = "ud"
}

enum AGLDTRel {
    NONE = "none",
    APOS = "apos",
    SUB = "sub",
    PRED = "pred",
    ADV = "adv",
    ATR = "atr"
}

enum XMLLang {
    GRC = "grc",
    LAT = "lat"
}

enum NodeType {
    Root = "root",
    Leaf = "leaf",
    Branch = "branch",
    NonRoot = "non-root",
    None = "none"
}

enum TreeTokenType {
    Root = "root",
    NonRoot = "non-root"
}
